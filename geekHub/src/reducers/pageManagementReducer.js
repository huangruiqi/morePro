const initalState = {
    homeBanner: ['第一张','第二张','第三张'],
    sourceBanner: ['第壹张','第贰张','第叁张']
}
export default (state = initalState, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
}
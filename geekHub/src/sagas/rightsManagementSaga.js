export function* agree(actions) {
    yield console.log(actions);
}

export function* disagree(actions) {
    yield console.log(actions);
}

export function* getInvitationCode(actions) {
    yield console.log(actions);
}

export function* getVerificationCode(actions) {
    yield console.log(actions);
}